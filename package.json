{
  "name": "erv-website-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:main": "npm run dev --workspace=frontend -- --host 0.0.0.0 --port 5173",
    "dev:app": "npm run dev --workspace=frontend -- --host 0.0.0.0 --port 5174",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:main\" \"npm run dev:app\"",
    "build": "npm run build --workspace=frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "allowScripts": {
    "bcrypt@5.1.1": true,
    "esbuild@0.21.5": true
  }
}
